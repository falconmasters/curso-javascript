// Crear un set
const colores = new Set(['rojo', 'verde', 'azul', 'rojo']);

// Agregar valores al set
colores.add('amarillo');
colores.add('amarillo');

// console.log(colores);

// colores.forEach((color) => {
// 	console.log(color);
// });

const numeros = new Set([1, 2, 3, 2, 2]);
console.log(numeros);
